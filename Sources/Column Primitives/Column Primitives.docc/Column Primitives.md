# ``Column_Primitives``

@Metadata {
    @DisplayName("Column Primitives")
    @TitleHeading("Swift Primitives")
}

The canonical storage-column compositions as named typealiases — `Column.Heap` (growable, heap-backed), `Column.Bounded` (fixed-capacity), and siblings — over `Buffer` × `Storage` × `Memory`.

## Topics
